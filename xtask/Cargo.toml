[package]
name = "xtask"
version = "0.1.0"
edition = "2021"
description = "Build tasks for Rune container runtime"

[dependencies]
clap = { version = "4", features = ["derive"] }
anyhow = "1"
xshell = "0.2"
